namespace NUnit.Commander.Configuration
{
    public enum DisplayMode
    {
        /// <summary>
        /// A full screen monitoring display
        /// </summary>
        FullScreen,
        /// <summary>
        /// A log friendly display if needing to redirect its output to stdout
        /// </summary>
        LogFriendly
    }
}
